# filing-agent 진행사항 — 2026-06-26

## 상태 요약
정상. 핵심 6단계(Phase 0~6) 구현 + **실데이터 라이브 검증 완료**. 라이브 실행에서 출처 검증 버그 2건을
발견·수정. ruff·pytest 108개 통과(키·DB·모델 없이).

## 완료한 일
- Phase 0 — 프로젝트 뼈대: uv·FastAPI `/health`·ruff·pytest·`.env` 분리
- Phase 1 — 데이터 수집 + 기본 RAG: DART 클라이언트·재무/서술 수집·청킹·임베딩·pgvector·`POST /ask`
- Phase 2 — RAG 고도화: 하이브리드 검색(벡터 + BM25 RRF 융합) + BGE-reranker-v2 리랭킹
- Phase 3 — 에이전트화: LangGraph ReAct + 도구 3종(`doc_search`·`financial_lookup`·`compute_change`)
- Phase 4 — 하네스: 검증 루프(숫자↔facts 결정론 대조) + 가드레일(인젝션·투자조언 선회) + 우아한 실패
- Phase 5 — 평가: 골든셋 9건(6유형) + 결정론 지표(정답률·라우팅·Hit@k·MRR) + 회귀 테스트 + `scripts/run_eval.py`
- Phase 6 — 관측·배포·CI: Langfuse 선택적 트레이싱 + Docker(앱 이미지 + compose `app`) + GitHub Actions(ruff·pytest)
- 문서: Phase 4·5·6 설계서(`docs/phase*_design.md`), 새 노트북 세팅 가이드(`docs/SETUP.md`) Phase 6 반영 갱신
- 라이브 검증: 실데이터 인제스트(9개 기업 15,666청크) + 에이전트 3종 질의 통과 + 평가 실행
  - 발견·수정한 버그 2건(모킹 테스트가 못 잡음): ① 숫자 답변 출처 검증이 산문만 봐서 전부 giveup →
    구조화 facts.source로 판정 ② 서술형 sources 빈 값 → doc_sources 채널로 surfacing
  - 평가 결과: 숫자 정답률 1.000, 라우팅 정확도 0.889(8/9) — combine(다기업 합산) 1건만 미달

## 막힌 점
없음. (Phase 6 구현 중 발견한 함정 2건은 모두 해결 — 아래 "참고" 참조)

## 다음 할 일
- (선택, 마무리) README에 데모 GIF·아키텍처 다이어그램 추가(평가 숫자는 이미 반영됨)
- (선택, 마무리) 면접 예상 질문 답변 정리(`docs/plan.md` 11장 기반)
- (선택, 개선) combine(다기업 합산) 라우팅 보강 — 에이전트가 "A사와 B사" 질의를 회사별 조회로 분해하게
- (선택, 확장) SEC EDGAR·MCP 서버화·멀티에이전트 등(`docs/plan.md` 12장)

## 집 노트북에서 이어서 하기
전체 환경 세팅은 `docs/SETUP.md`에 있다. 두 노트북 모두 이미 세팅돼 있다면 아래 3단계로 이어받는다.

```powershell
git pull                          # 최신 코드 받기
uv sync                           # 새 의존성(langfuse v4 등) 반영
docker compose up -d pgvector     # DB만 띄운다(서비스명 꼭 붙일 것 — Phase 6에서 app 추가됨)
```

- 코드만 빠르게 확인하려면(키·DB 불필요): `uv run pytest` → 102개 통과.
- RAG·에이전트를 실제로 돌리려면: `.env`에 키가 있어야 하고(노트북마다 직접), `uv run python scripts/ingest_all.py`로
  데이터를 재적재한다. `data/`·pgvector 내용은 git에 없어 노트북마다 1회 재생성한다.
- `.env`에 Langfuse 키는 선택. 없으면 트레이싱만 꺼지고 앱은 정상 동작한다.

## 참고: Phase 6에서 자체 검토로 잡은 함정 2건
- 무음 실패: pydantic-settings는 `.env`를 `os.environ`에 안 넣는데 Langfuse/litellm 콜백은 환경변수에서 키를 읽음
  → 활성화 시 Settings→os.environ 전파로 해결(안 하면 에러 없이 트레이싱이 안 켜짐).
- 버전 충돌: 설치된 langfuse v4는 OTEL-native라 litellm의 v2용 `"langfuse"` 콜백과 깨짐 → `"langfuse_otel"` 사용.

## 지표
- 테스트: 108개 통과(Phase 6: 102 → 라이브 버그 수정 +6)
- 구현 단계: 6/6 (핵심 단계 전부) + 실데이터 라이브 검증 완료
- 평가: 숫자 정답률 1.000 · 라우팅 정확도 0.889(9건 골든셋, 방향성 지표)
