# OpenDART 인증키 — https://opendart.fss.or.kr 에서 무료 발급 후 아래 값에 채운다.
# 이 파일은 '예시'다. 실제 키는 .env 에 넣으며 .env 는 git 에 커밋되지 않는다.
DART_API_KEY=

# (선택) 아래 기본값을 덮어쓸 때만 주석을 풀어 설정한다.
# DART_REPORT_CODE=11011   # 11011=사업보고서(연간), 11012=반기, 11013=1분기, 11014=3분기
# DART_FS_DIV=CFS          # CFS=연결, OFS=별도
